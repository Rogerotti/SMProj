<Window x:Class="VJPlayer.Views.EffectPicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VJPlayer.Views"
        mc:Ignorable="d"
        Title="EffectPicker" Height="300" Width="300">
    <Grid>
        <ListBox x:Name="effectsListBox" HorizontalAlignment="Left" Height="270" VerticalAlignment="Top" Width="292">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="{Binding Name}" />
                        <CheckBox Grid.Column="1" IsChecked="{Binding IsActive}" Click="CheckBox_Click" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
